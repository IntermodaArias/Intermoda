<UserControl xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"  x:Class="Intermoda.Wpf.Controls.TabHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <StackPanel Orientation="Horizontal" Margin="2" HorizontalAlignment="Center">
        <Label Margin="2" VerticalAlignment="Center" FontWeight="DemiBold" Content="{Binding Path=HeaderText}"/>
        <telerik:RadButton Background="{x:Null}" BorderBrush="{x:Null}" Foreground="{x:Null}"
                           Command="{Binding Path=CloseTabItemCommand}">
            <telerik:RadButton.Content>
                <Image Margin="2" Width="16" Height="16" VerticalAlignment="Center" Source="Images/CancelRed.png"/>
            </telerik:RadButton.Content>
        </telerik:RadButton>
    </StackPanel>
</UserControl>
