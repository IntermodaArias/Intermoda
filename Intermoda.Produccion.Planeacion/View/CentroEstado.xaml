<UserControl x:Class="Intermoda.Produccion.Planeacion.View.CentroEstado"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="30" d:DesignWidth="150">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="75"/>
            <ColumnDefinition Width="75"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Grid.Column="0" Margin="2" HorizontalAlignment="Right" VerticalAlignment="Center" 
                   Text="{Binding Path=Entrada, StringFormat='{}{0:#,#}'}"/>
        <TextBlock Grid.Row="0" Grid.Column="1" Margin="2" HorizontalAlignment="Right" VerticalAlignment="Center" 
                   Text="{Binding Path=Salida, StringFormat='{}{0:#,#}'}"/>
    </Grid>
</UserControl>
